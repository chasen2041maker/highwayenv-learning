# 学习历史与实验依据

当前接续点见 [PROGRESS.md](../PROGRESS.md)。后续按时间追加实质变化，不把讲解、助手验证和学习者独立掌握混写。以下首次归档依据当前会话回顾，不给旧实验编造精确发生时间。

## 2026-09-23｜实践起点与运行环境

- 学习者希望直接从实践入门自动驾驶，在 VS Code 中自己操作；使用中文及中文拼音提问。
- 助手检查项目、解释命令，学习者安装和运行。pip 安装因 `noise` 需要 MSVC 失败；按建议先用 Conda 安装 `noise` 后，学习者提供项目安装成功输出。
- 学习者创建 `demo.py` 并确认看到道路和车辆窗口。当前使用本机 `py310` 环境，未迁移或重建项目。
- 随机动作改为固定 `action = 1` 后，学习者报告直行并碰撞；已讲解保持当前车道和目标速度不包含避障判断。

## 2026-09-23｜从车辆表到减速规则

- 学习者打印 5×5 车辆观察数据，要求把数字解释得更直白。助手把数据转换为自车 90 km/h、同车道前方约 82.9 米的车辆约 80.3 km/h 等具体含义。
- 已讲：各列 `presence/x/y/vx/vy`，自车与其他车辆行的区别，相对速度的负号，以及默认归一化。用户随后表示理解，但尚未用新的数值独立说明。
- 学习者按指导实现前方 40 米内、横向差小于 2 米、比自车慢则减速，并反馈成功。
- 随后发展为距离规则：小于 40 米选择 SLOWER，大于 60 米选择 FASTER，其余 IDLE；同时打印实际速度。当前代码检查确认存在该实现。
- 每一步重新使用 `obs` 选择动作，属于手写反馈规则；尚未训练神经网络。

## 2026-09-23｜成绩单与用户运行证据

执行者：学习者；方式：助手提供逐步指导，学习者在 VS Code 中修改和运行。

实际命令：`python .\demo.py`。来源：用户粘贴的运行记录，现保存在 [原始日志](evidence/2026-09-23-user-driving-log.txt)。

记录中的完整回合：

| 回合 | 仿真时间（秒） | 累计奖励 | 结束情况 |
| --- | ---: | ---: | --- |
| 1 | 40.0 | 30.04 | 达到时间上限，无碰撞 |
| 2 | 40.0 | 31.27 | 达到时间上限，无碰撞 |
| 3 | 40.0 | 26.69 | 达到时间上限，无碰撞 |
| 4 | 40.0 | 29.36 | 达到时间上限，无碰撞 |

完整回合平均奖励：29.34。第 5 局只包含部分输出，不计入完整回合统计；没有凭终端返回推断其中断原因。以上是对用户日志的分析，不是助手另行复现了同一组交通场景。

未记录固定随机种子，不能据此做严格的策略对照或跨场景可靠性结论。另一条较早的用户成绩单为 40 秒、无碰撞、奖励 32.61，不混入这批 4 局的均值。

学习者询问 `round`，已用实际打印语句解释保留小数位数以及原累计值未被打印操作修改。尚未收到独立解释，记录为已讲解。

## 2026-09-23｜发现最低目标速度与低速配置教学

用户日志中多次打印“减速”，实际速度仍保持 72 km/h。源码核实：`MDPVehicle.DEFAULT_TARGET_SPEEDS` 为 `[20, 25, 30]` m/s，`DiscreteMetaAction` 允许通过 `target_speeds` 覆盖。

已向学习者给出 `gym.make` 配置片段，将档位改为 `[0, 5, 10, 15, 20, 25, 30]` m/s，并建议并排打印实际速度和目标速度。讲解了配置字典、档位单位和目标/实际的区别。

### 助手独立验证的范围

执行者：助手，不计作学习者完成本课。使用当前本机 `py310`，`highway-v0`，无图形窗口，其他车辆数为 0，`seed=0`，连续 7 次 `env.step(4)`。对比两组档位，保留验证结果于 [验证记录](evidence/2026-09-23-speed-targets-check.json)。

| 配置 | 每一步实际速度（km/h，保留 1 位小数） | 最后目标速度 |
| --- | --- | --- |
| `[20, 25, 30]` | 75.1、72.5、72.1、72.0、72.0、72.0、72.0 | 72 km/h |
| `[0, 5, 10, 15, 20, 25, 30]` | 75.1、57.6、39.7、21.7、3.7、0.6、0.1 | 0 km/h |

该实验只说明自定义速度档位能被执行，没有验证真实交通中的跟车效果、变道或避免碰撞。归档开始时首次检查，学习者 `demo.py` 仍使用默认 `gym.make`；收尾时发现文件已有新修改，见下面的补充记录。

## 2026-09-23｜建立持续教学记录

用户要求：建立 `AGENTS.md` 写清教学目的，定期保存进度，参考 `vla_basic`，强调直接从实践学习。

本次已建立教学约定、当前进度、路线图和本历史文件；归档用户实验日志和助手验证输出。参考版本 `4503a9cc9d0b67add5e85c28aa5d75e73f2725a7`，采用项目驱动与按证据记录的组织思路，没有导入参考项目的个人能力档案或任务限制。

保存方式：每完成一个教学小节、收到新实验/理解证据、解决报错或切换课题时，随活跃对话更新本地文件。不代表后台定时保存、Git 提交或远端同步。

本次文档工作未更改 `demo.py`，也未把低速配置课标记为学习者完成。下一次接续仍围绕降低最低目标速度；具体最新状态以根目录进度文件为准。

### 21:04 收尾检查：工作区已有低速配置

助手重新读取 `demo.py`，发现文件于本地 21:02:03 更新，已加入 `[0, 5, 10, 15, 20, 25, 30]` 档位和 step 后的目标速度打印。本轮助手没有写入该脚本，因此按学习者当前工作区的代码证据记录为“已修改，待运行结果”，不再沿用首次检查时的“尚未修改”。

尚未收到学习者针对这一版的运行日志或目标/实际速度解释。已同步根目录进度；下次应解读用户运行结果，而不是让用户重复粘贴已有配置。

## 2026-09-23 21:07｜加入教学节点进度维护技能

- 用户要求增加技能维护进度，并明确选择“每个教学小节结束或收到新实验结果时维护”。
- 助手按 skill-creator 规范创建个人技能 `$highway-learning-progress`，位置为 `C:\Users\Administrator\.codex\skills\highway-learning-progress`；包含 `SKILL.md` 和 `agents/openai.yaml`，允许自动选择。
- 已在项目 `AGENTS.md` 接入调用规则，覆盖小节结束、新实验/理解反馈、错误解决、课题切换及暂停接续。按事实更新当前进度和历史记录，不生成第二份进度档案。
- 实际校验：使用本机 `py310`、`-X utf8` 运行 skill-creator 的 `quick_validate.py`，退出码 0，输出 `Skill is valid!`；另核对调用配置。该结果是技能结构验证，不是驾驶实验结果。
- 本次按技能完成一次文档同步。未修改 `demo.py`，未新增驾驶运行证据，未提升学习状态；低速档位仍为“代码已修改，待用户运行结果和解释”。
- 未创建固定时间后台自动任务，保存发生在活跃教学对话的上述节点。

## 后续记录格式

```text
日期与本次问题：
实际改动及执行者（学习者/助手/共同）：
命令与实验条件（必要时附版本、种子）：
真实结果与证据文件：
已经讲解、用户自述理解、用户独立解释分别是什么：
仍待核对的问题：
本次如何更新 PROGRESS.md：
```
