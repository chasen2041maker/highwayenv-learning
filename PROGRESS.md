# 当前学习进度

更新时间：2026-09-23T21:13:14+08:00，Asia/Shanghai。记录依据：当前会话、学习者提供的日志，以及本地文件和 GitHub 核验。本次新增的是仓库上传结果，驾驶课实践/理解状态未升级。

这是唯一的当前接续点。教学约定见 [AGENTS.md](AGENTS.md)，长期方向见 [路线图](learning/ROADMAP.md)，历史证据见 [学习记录](learning/LEARNING_LOG.md)。

## 当前课题：增加低速档位

**问题：为什么程序一直选择减速，车速却保持在 72 km/h？怎样让它继续减速？**

| 项目 | 当前事实 |
| --- | --- |
| 用户选择 | 接着“让车能减到 72 以下”的实验学习，直接从实践入门 |
| 已有程序 | `demo.py` 根据观察到的最近同车道前车距离，在减速、保持、加速中选择；记录每局时间、碰撞和累计奖励 |
| 文件检查 | 收尾检查确认用户工作区已加入 `[0, 5, 10, 15, 20, 25, 30]` 自定义档位，且在 step 后并排打印实际和目标速度；文件修改时间为 21:02:03 |
| 本课实践状态 | 用户代码已修改，待其运行结果；助手独立小实验已验证配置有效，但不能替代用户带交通场景的实际运行 |
| 本课学习状态 | 已讲解默认速度档位、配置方式和目标/实际速度区别；尚无学习者对本课的独立解释 |
| 下一步 | 请用户运行现有代码或查看其提供的最新输出，重点解释低于 72 km/h 时的目标和实际速度；不再重复要求添加已经存在的配置 |

本次文档整理没有修改用户的 `demo.py`。不要把“示例已给出”记成“用户已经完成”。

## 下一次从这里继续

1. 当前 `demo.py` 已有以下配置；再次接续时先核对用户是否有更新，不要求重复添加：

   ```python
   config={
       "action": {
           "type": "DiscreteMetaAction",
           "target_speeds": [0, 5, 10, 15, 20, 25, 30],
       }
   }
   ```

2. 这是米/秒，对应 `0、18、36、54、72、90、108 km/h`；保持档位等间隔。原来默认只有 `[20, 25, 30]`。
3. 实际速度和 `env.unwrapped.vehicle.target_speed * 3.6` 的目标速度打印已经加入。目标速度是诊断信息；驾驶决策仍从 `obs` 读取附近车辆。
4. 学习者运行 `python demo.py`，观察连续减速时能否出现低于 72 km/h 的实际速度。实际道路上是否连续减速，仍取决于原有距离规则。
5. 结合用户结果只核对一个问题：“为什么目标速度已经是 54，实际速度可能暂时仍是 57.6？”先用现象解释，再按需带读控制器。
6. 收到证据后更新本页与历史记录。随后自然带读 `obs → action → env.step() → 新 obs`，解释为什么每一步都重新观察；不要无故重新安装环境或重讲整条路线。

本课成功证据：学习者的配置和日志显示目标速度可降至 20 m/s 以下，实际速度随之下降；能用自己的话区分目标与实际。不要求在有交通的随机场景中一定减到零，也不声称扩大档位必然消除碰撞。

## 已有实践与理解记录

| 内容 | 实践证据 | 学习理解证据 |
| --- | --- | --- |
| 安装和启动 | 学习者提供安装成功输出并确认看见车辆窗口 | 能按指导在 VS Code 终端运行；独立环境排错能力未核对 |
| 随机动作和固定 IDLE | 学习者运行并描述直行后碰撞 | 已讲解 IDLE 保持目标、不会自动避障；尚无独立原理复述 |
| 车辆观察表 | 学习者粘贴 5×5 数据，要求更直白解释 | 用实际距离和车速翻译后，用户表示理解；新的数值换算尚未核对 |
| 遇慢车减速 | 学习者修改并反馈跑通；当前文件保留旧规则作为注释 | 有辅助实践证据，独立编写尚未核对 |
| 距离规则：40/60 米 | 当前代码和运行记录显示三种动作都有执行 | 已讲解距离缓冲区；尚未要求用户独立设计阈值实验 |
| 每局成绩单 | 4 个完整回合均无碰撞且达到 40 秒；平均累计奖励 29.34 | 已讲解 reward 累加和结束原因；用户问过 `round`，已经得到解释 |
| 低速配置 | 用户文件已配置并加入目标速度打印，待运行日志；助手零交通独立验证通过 | 已讲解，待学习者运行和解释 |

4 个完整回合不是跨场景可靠性证明。第 5 局日志不完整，没有将其计入统计。原始记录与每局得分见历史文件。

## 当前实现的已知边界

- 当前默认观察包括自车和至多 4 辆附近车；“没有观察到同车道前车”不代表前方一定为空。
- 当前规则只按最近前车距离决定动作，没有使用速度差、反应时间或完整的跟车控制。
- `x * 200`、`y * 16` 是当前默认 4 车道、默认归一化范围下的换算；修改观察或道路配置后必须重新核对。
- 自车一行记录自身状态；其他车辆行在当前设置下是相对位置和相对速度。
- 默认决策频率 1 Hz，环境时限 40 秒；`range(300)` 是整个脚本的循环预算，可能在某局中途结束。
- 旧代码仍以多行字符串保存在 `demo.py`；本轮只建立教学记录，未替学习者清理或重写。
- 还没有系统完成源码带读、独立规则实现、固定种子评测、变道或 DQN 训练。

## 当前环境

以下是本机记录，不应在其他机器上盲目照搬。

- Windows / VS Code / PowerShell；项目为 HighwayEnv 源码仓库。
- Conda 环境：`py310`；Python 3.10.20。
- 解释器：`D:\miniconda\envs\py310\python.exe`。
- 2026-09-23 核实版本：highway-env `1.12.2.dev0`、gymnasium `1.3.0`、numpy `1.26.4`、pygame-ce `2.5.8`、noise `1.2.2`。
- 正常命令：在项目目录、已激活 `py310` 的终端运行 `python demo.py`。
- 曾遇到 pip 构建 `noise` 缺少 MSVC；使用 `conda install -c conda-forge noise` 后，再执行 `python -m pip install -e .` 成功。
- HighwayEnv 上游基线：`c978b198cddcbd27aa62513fc3e6a92bb53f9f5d`。用户的 `demo.py` 已随个人教学资料提交并上传，首次上传提交为 `d39c77934e119f24f5ae5ebfc209b5ae1a635078`。

## 保存节奏

每完成一个小节、收到新实验/理解证据或发生课题切换，助手在该轮结束前更新本页，并在历史记录追加结果。持续采用“实践状态”和“学习理解”分别记录的方式。

用户于 2026-09-23 明确选择“每个教学小节结束或收到新实验结果时维护”。已创建技能 `$highway-learning-progress`，其 [仓库内入口](.agents/skills/highway-learning-progress/SKILL.md) 与本机个人安装内容一致，已通过 skill-creator 格式校验，自动选择已启用，项目 `AGENTS.md` 已接入调用规则。下一次恢复教学先核对本页，有新增事实时再落盘；无变化不重复添加日志。没有配置固定时间的后台任务。

## 代码与资料仓库

用户指定的个人仓库：[chasen2041maker/highwayenv-learning](https://github.com/chasen2041maker/highwayenv-learning)。已按此次授权上传当前项目、`demo.py`、教学文档、实验依据及仓库内进度维护技能，保留 HighwayEnv 上游来源、历史和许可证。

首次上传提交：[d39c779](https://github.com/chasen2041maker/highwayenv-learning/commit/d39c77934e119f24f5ae5ebfc209b5ae1a635078)。已通过 GitHub API 核对当时 `main` 与本地提交一致，并确认主要文件存在。本地 `origin` 指向个人仓库，`upstream` 指向 Farama Foundation 的 HighwayEnv，`main` 跟踪 `origin/main`。

此次是代码与资料归档：已检查练习脚本语法、文档链接和技能结构；使用 `[skip ci]` 避免首次导入触发上游整套 CI/部署流程，没有宣称这些流程已经通过。进度技能仍按教学节点维护本地记录，后续提交或推送按实际任务授权执行。
